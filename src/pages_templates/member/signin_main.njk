<div class="page_section-inner">
    <div class="page_section-content container">
        <div class="signin_main-wrapper">
            <div class="page_section-bd signin_main-bd pages-wrapper-v">
                <div class="pages-heading">
                    <div class="inner">
                        <h2 class="signin_main-heading-title">
                            <span class="image_replacement image_replacement-signin text-hide" style="background-image: url({{IMG_PATH}}image_replacement-signin.svg)">
                                SIGN IN
                            </span>
                            <div class="txt">註冊會員</div>
                        </h2>
                    </div>
                </div>
                <div class="pages-wrapper-v-pattern"></div>
                <div class="pages-main">
                    <div class="inner">
                        <!-- action 示意: 註冊會員表單送出後，導頁至會員專區頁(account_memberinfo) -->
                        <form class="signin_frm {{PROJECT_NAME}}_frm" action="account_memberinfo.php" method="POST">
                            <div class="form-group">
                                {% set fieldSet = {
                                field_class: PROJECT_NAME + "_field-v",
                                name: "email",
                                isRequired: 1,
                                label: "EMAIL(帳號)",
                                placeholder: "example@justplay.com"}%}
                                {{ field.input(PROJECT_NAME, fieldSet) }}
                            </div>
                            <div class="form-group">
                                {% set fieldSet = {
                                field_class: PROJECT_NAME + "_field-v password_field",
                                id: "password_field",
                                name: "password",
                                isRequired: 1,
                                label: "密碼",
                                placeholder: "請輸入密碼"}%}
                                {{ field.password(PROJECT_NAME, fieldSet) }}
                            </div>
                            <div class="form-group">
                                {% set fieldSet = {
                                field_class: PROJECT_NAME + "_field-v password_field",
                                id: "password_check_field",
                                name: "password_check",
                                isRequired: 1,
                                label: "確認密碼",
                                placeholder: "請再次確認密碼"}%}
                                {{ field.password(PROJECT_NAME, fieldSet) }}
                            </div>
                            <div class="form-group">
                                {% set fieldSet = {
                                field_class: PROJECT_NAME + "_field-v",
                                name: "name",
                                isRequired: 1,
                                label: "姓名",
                                placeholder: "請輸入姓名"}%}
                                {{ field.input(PROJECT_NAME, fieldSet) }}
                            </div>
                            <div class="form-group">
                                {% set fieldSet = {
                                field_class: PROJECT_NAME + "_field-v",
                                name: "phone",
                                isRequired: 1,
                                label: "電話",
                                placeholder: "請輸入電話"}%}
                                {{ field.input(PROJECT_NAME, fieldSet) }}
                            </div>
                            <div class="form-group">
                                {% set fieldSet = {
                                field_class: PROJECT_NAME + "_field-v date_field",
                                name: "birthday",
                                isRequired: 1,
                                label: "生日",
                                placeholder: "請輸入生日"}%}
                                {{ field.input(PROJECT_NAME, fieldSet) }}
                            </div>
                            <div class="form-group">
                                {% set fieldSet = {
                                field_class: PROJECT_NAME + "_field-v",
                                name: "address",
                                isRequired: 1,
                                label: "地址",
                                placeholder: "請輸入地址"}%}
                                {{ field.input(PROJECT_NAME, fieldSet) }}
                            </div>

                            <div class="form-btns">
                                <button type="submit" class="{{PROJECT_NAME}}_btn {{PROJECT_NAME}}_btn-Pri">
                                    註冊
                                    <svg class="svgicon" data-src="{{IMG_PATH}}icons/icon-more-arrow-2.svg"></svg>
                                </button>
                            </div>
                            <div class="form-ft">
                                <div class="txt txt-Gray-03">已經有帳號了？</div>
                                <a href="login.php" class="link-Black txt-Pri td-u">立即登入</a>
                            </div>
                        </form>
                    </div>

                </div>
            </div>
        </div>
    </div>
</div>
<!-- /.page_section-inner -->